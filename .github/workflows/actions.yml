name: Build Site
run-name: Rebuilding the site 🚀
on:
  #  pull_request:
  #    branches:
  #      - main
  #    types:
  #      - closed
  push:
    branches:
      - jamcmich/test-merge

jobs:
  build:
    #    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup Git config
        run: |
          git config --global user.email "jacobmcmichael@gmail.com"
          git config --global user.name "Jacob McMichael"
      - name: Run deployment script
        run: |
          chmod +x ./vue-ts/deploy.sh
          ./vue-ts/deploy.sh
          shell: bash